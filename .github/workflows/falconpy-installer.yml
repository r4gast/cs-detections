name: Falcon Test

on:
  push:
    branches:
      - main

jobs:
  falcon-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install FalconPy
        run: 
          pip3 install crowdstrike-falconpy

      - name: Run Script
        run: python scripts/falconpy.py
